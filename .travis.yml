language: cpp
dist: xenial

before_script:
    - sudo apt-get update -qq
    - sudo apt-get install -qq -y git libxml2 libxml2-dev libssl-dev libprotobuf-dev protobuf-compiler python-protobuf python-networkx python-nacl python-pip python-tk python-dev python-requests python-crypto build-essential openssl swig

script:
    - ./configure && make

branches:
    only:
        - v2-routing

 notifications:
    slack: xia-cmu:i6KX0DyBuYJQUf5c5RUu4AOZ


