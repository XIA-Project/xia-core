language: cpp

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq git libxml2 libxml2-dev libssl-dev libprotobuf-dev protobuf-compiler python-protobuf python-networkx python-nacl python-pip python-tk python-dev python-requests python-crypto build-essential openssl swig

script: ./configure && make



